# 暴露监控
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: "*"
    jmx:
      exposure:
        include: "*"
  metrics:
    export:
      datadog:
        application-key: ${spring.application.name}
spring:
  application:
    name: prometheus-center